# SensorCalibration
A PLC program to calibrate an oxygen sensor in a coal mine on user request by sampling two reference gases with fixed oxygen levels, calculating the average readings of each of the samples, and using those averages to tune the sensor's scaling parameters.
